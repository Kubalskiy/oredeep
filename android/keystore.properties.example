# Скопируй в android/keystore.properties (файл в .gitignore — НЕ коммить).
# storeFile — путь относительно папки android/

storeFile=oredeep-release.keystore
storePassword=CHANGE_ME
keyAlias=oredeep
keyPassword=CHANGE_ME
