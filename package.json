{
  "name": "oredeep",
  "version": "0.12.0",
  "description": "ORE DEEP — idle-добыча в гномьих чертогах (рескин баланса Idle Boxer)",
  "private": true,
  "scripts": {
    "build": "mkdir -p www && cp index.html www/index.html",
    "sync": "npm run build && npx cap sync",
    "android": "npm run sync && npx cap open android",
    "test": "node tools/run_tests.js",
    "art": "python3 tools/gen_pixel_art.py"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.0",
    "@capacitor/core": "^6.1.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.0"
  }
}
